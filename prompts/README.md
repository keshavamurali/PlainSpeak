# Prompts

Add agent prompts here as `.md` files. Each file corresponds to an agent type (e.g. `planner.md`, `coder.md`, `reviewer.md`).

Reference these in `agents/config/agents.yaml` via the `prompt_file` config key.

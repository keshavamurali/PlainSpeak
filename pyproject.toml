[project]
name = "plainspeak"
version = "1.0.0"
description = "PlainSpeak API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "networkx>=3.0.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
    "mcp[cli]>=1.6.0",
    "sse-starlette>=2.0.0",
    "google-genai>=1.0.0",
    "json-repair>=0.7.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
dev-dependencies = []
